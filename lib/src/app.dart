import 'package:flutter/material.dart';

class App extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
      title: 'Login mazseh',
      home: Scaffold(
        body: Text('Hmmmmmm..'),
      ),
    );
  }
}
